

const SectionTitle = ({title}) => {
    return (
        <h1 className="font-bold max-w-7xl text-xl md:text-3xl py-10 px-5">
            {title}
        </h1>
    );
};

export default SectionTitle;